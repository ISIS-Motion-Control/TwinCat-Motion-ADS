<UserControl x:Class="TwinCat_Motion_ADS.MVVM.View.SettingControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Horizontal">
        <TextBlock x:Name="SettingTitle" HorizontalAlignment="Left"
                   TextAlignment="Right"
                   Margin="0,0,20,0"
                                       VerticalAlignment="Center" 
                                       Width="220"
                                       FontSize="16"
                                       Text="{Binding SetName}"/>
        <TextBox x:Name="SettingValue" 
                                     HorizontalAlignment="Left" 
                                     Text="{Binding SetValue}"  
                                     VerticalAlignment="Center" 
                                     Width="200"
                             Margin="0,0,0,0" FontSize="16"/>
    </StackPanel>
</UserControl>
